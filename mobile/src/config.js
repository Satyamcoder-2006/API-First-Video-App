// Update this with your deployed backend URL or local IP for device testing
// For local testing on physical device: use your computer's IP (e.g., "http://192.168.1.100:5000")
// For deployed backend: use your Render/Railway URL (e.g., "https://yourapp.onrender.com")
export const API_BASE_URL = __DEV__
  ? "http://localhost:5000"
  : "https://YOUR_DEPLOYED_BACKEND_URL";

